Requerimientos:

- Instalar Docker y Docker Compose

Para levantar el servicio de N8N:

docker compose up

Ejecutar el script desde local: `curl -X POST http://localhost:5000/run-script`

ejecutar el script desde el contenedor de n8n: `curl -X POST http://python-script/run-script`

